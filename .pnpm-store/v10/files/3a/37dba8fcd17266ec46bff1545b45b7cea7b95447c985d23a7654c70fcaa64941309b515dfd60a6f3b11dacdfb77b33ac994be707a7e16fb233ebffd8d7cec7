import { Event, Attribute } from '@cosmjs/stargate/build/events';
export declare function parseMoveAttributes(attributes: readonly Attribute[]): {
    [k: string]: string;
};
export interface Change {
    amount: string;
    metadata: string;
}
export declare const accumulateChanges: (changes: Change[]) => Change[];
export declare function calcChangesFromEvents(events: Event[], fee: {
    amount: string;
    metadata: string;
}, address: string): Change[];
